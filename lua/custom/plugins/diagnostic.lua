return {
--   'rachartier/tiny-inline-diagnostic.nvim',
--   event = 'VeryLazy',
--   config = function()
--     require('tiny-inline-diagnostic').setup {
--       hi = {
--         mixing_color = '#e1e2e7', -- Can be None or a hexadecimal color (#RRGGBB). Used to blend the background color with the diagnostic background color with another color.
--       },
--     }
--     vim.diagnostic.config { virtual_text = false }
--     vim.keymap.set('n', '<leader>cd', require('tiny-inline-diagnostic').toggle, { desc = 'Toggle [C]ode [D]iagnostics' })
  -- end,
}
